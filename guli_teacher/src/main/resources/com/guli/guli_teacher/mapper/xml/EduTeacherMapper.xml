<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guli.guli_teacher.mapper.EduTeacherMapper">
    <select id="initIndexTeacher" resultType="EduTeacher">
        select * from edu_teacher where is_deleted = 0 order by sort limit 0,4;
    </select>
</mapper>
